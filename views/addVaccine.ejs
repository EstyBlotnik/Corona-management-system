<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <link rel="stylesheet" type="text/css" href="newMember.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f5f5f5;
        }

        .container {
            width: 60%; /* Adjust the width as needed */
            margin: 20px auto;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px; /* Add some margin below the heading */
        }

        .form-group {
            margin-bottom: 10px; /* Decrease the margin between form groups */
        }

        input[type="text"],
        input[type="date"],
        input[type="tel"] {
            width: calc(100% - 16px); /* Calculate width to accommodate padding */
            padding: 8px;
            border-radius: 5px;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }

        
        .delete-button {
            background-color: #066f7d;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .delete-button:hover {
        background-color: #078090;
        }
        .submit{
            margin: 0 auto;
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>Add a vaccine</h1>
        <form id="memberDetailsForm" action="/member/details" method="POST">
            <button class="delete-button back-button">Back</button>
            <input type="hidden" name="_id" value="<%=member._id %>">
        </form>
        <br>
        <form id="userDetailsForm" action="/member/addVaccine/details" method="POST">
            <fieldset>
                <legend>Vaccine Details</legend>                
                <div class="form-group">
                    <label for="date">Date of vaccination:</label>
                    <input type="date" id="date" name="date" required>
                </div>
                <div class="form-group">
                    <label for="producer">Producer:</label>
                    <input type="text" id="producer" name="producer" required>
                </div>
            </fieldset>
            <br>
            <button type="submit" class="delete-button submit">Submit</button>
            <input type="hidden" id="date" name="_id" value="<%= member._id  %>">
        </form>
    </div>
</body>
</html>
